#!/usr/bin/node

/* create a class named Rectangle
 * instance attribute <width> and <height>
 */

module.exports = class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
};
